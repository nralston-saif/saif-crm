# Supabase Configuration
# Get these values from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (for server-side operations like webhooks)
# Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Webhook Authentication (optional but recommended)
# Generate a random string: openssl rand -hex 32
# Add this to your JotForm webhook URL as ?secret=YOUR_SECRET
WEBHOOK_SECRET=your-webhook-secret-here
